{
  "schema": 1,
  "name": "promptaudit",
  "github": "https://github.com/SuperMarioYL/promptaudit",
  "host": "promptaudit.lei6393.com",
  "lang": {
    "primary": "zh",
    "toggle": true
  },
  "accent": [
    "#e6a144",
    "#f59e0b",
    "#7c2d12"
  ],
  "hero": {
    "eyebrow": {
      "zh": "扫依赖树 · 抓注入 · 护住 Agent",
      "en": "Scan deps · Catch injection · Shield your agent"
    },
    "headline": {
      "zh": "一条命令\n扫出依赖里瞄向 Coding Agent 的注入指令",
      "en": "One command catches the\nprompt-injection hiding in your dependencies"
    },
    "sub": {
      "zh": "解析你的 npm + PyPI 依赖树，抓取每个包的 README 与文档文本，用一套经过实战沉淀的注入指令规则集逐条比对 —— 命中即报文件、行号、片段、严重级别。jqwik 事件的武器化 README 会被原样标红报出。",
      "en": "Resolves your npm + PyPI dep tree, pulls each package's README and doc text, matches a battle-tested injection-payload rule set, and reports file, line, snippet, and severity on every hit. The jqwik incident's weaponized README is flagged red verbatim."
    },
    "command": "promptaudit scan .",
    "cta": {
      "zh": "看它怎么扫",
      "en": "See it scan"
    },
    "ctaGithub": {
      "zh": "GitHub",
      "en": "GitHub"
    },
    "scroll": {
      "zh": "往下看原理",
      "en": "How it works"
    }
  },
  "advantages": [
    {
      "title": {
        "zh": "扫的是依赖真正塞进 Agent 上下文的文本",
        "en": "Scans the text deps actually feed your agent — not version numbers"
      },
      "body": {
        "zh": "扫的是依赖真正塞进 Agent 上下文的文本 —— README、文档、jqwik 这类会被原样加载进 prompt 的内容，逐文件命中即报。不管 sdist 有多少个成员文件，都从头扫到尾，不会在 48 个文件处静默截断。",
        "en": "Scans the text deps actually inject into your Agent's context — README, docs, jqwik-style content that gets loaded verbatim into the prompt, reported per-file on hit. However many member files an sdist has, every one is scanned end to end — never silently truncated at 48."
      },
      "chips": [
        "› 逐文件命中即报",
        "› 多成员不截断"
      ]
    },
    {
      "title": {
        "zh": "命中的注入指令，直接给到文件、行号、片段",
        "en": "Every hit points at the file, line, and snippet"
      },
      "body": {
        "zh": "不是一句“发现 3 条可疑”——而是精确到哪个包、哪个文件、第几行、带上下文片段。critical 级别直接让 CI 失败，--json 输出给流水线机器消费。",
        "en": "Not a vague '3 suspicious hits' — but exactly which package, which file, which line, with a context snippet. Critical findings fail CI, and --json hands the pipeline machine-readable output."
      },
      "chips": [
        "› file:line:snippet",
        "› critical → CI 失败",
        "› --json 机器可读"
      ]
    },
    {
      "title": {
        "zh": "匹配不到版本、拉不下来、工作区包——覆盖报告都说真话",
        "en": "No matching version, fetch failure, workspace package — the coverage report tells the truth"
      },
      "body": {
        "zh": "一个 npm 范围（^/~ /x-range）匹配不到任何已发布版本，或 ~= 锁定版本解析失败，工具不会偷偷去扫 registry latest（一个你根本装不上的版本）然后报干净——而是记成「没扫到」。一个 404 / 被 yank 的包照实记成没扫到；monorepo 里工作区自身的包（apps/web、packages/foo）也不会被当成 npm 依赖去拉、再报成 version_not_found 误失败你的 CI。带 --hash 的 pip-compile 锁定文件也不再因为解析失败而漏扫整棵树。",
        "en": "An npm range (^/~ /x-range) that matches no published version, or a ~= pin whose lookup failed, is never silently scanned at the registry latest (a version you can't even install) and passed clean — it's recorded as 'not scanned'. A 404 / yanked package is recorded as not-scanned; a workspace's own package in a monorepo (apps/web, packages/foo) is never fetched as an npm dependency and reported as version_not_found to false-fail your CI. A pip-compile --hash-pinned requirements file no longer under-scans the whole tree from a parse failure."
      },
      "chips": [
        "› 范围无匹配不当干净",
        "› 404/yank 照实记",
        "› 工作区包不误报",
        "› --hash 锁定不漏扫"
      ]
    },
    {
      "title": {
        "zh": "审计你真正发布到的平台，不卡在扫描机本身",
        "en": "Audit the platform you ship to — not the scanner's host"
      },
      "body": {
        "zh": "Windows 专属依赖、旧 Python 版本依赖默认会被扫描机环境跳过。--target-platform windows / --target-python 3.8 让你从 Linux CI 审计 Windows 安装，跳过的依赖也会作为覆盖率缺口报出来。",
        "en": "Windows-only deps and old-Python deps get skipped by the scanner host by default. --target-platform windows / --target-python 3.8 audit a Windows install from a Linux CI runner — and skipped deps still surface as coverage gaps."
      },
      "chips": [
        "› --target-platform",
        "› --target-python",
        "› 跨平台覆盖"
      ]
    }
  ],
  "scene": {
    "kind": "hub-nodes",
    "nodes": [
      "npm",
      "PyPI",
      "README",
      "docstring",
      "summary"
    ]
  },
  "cta": {
    "headline": {
      "zh": "装一条命令，挡住武器化依赖",
      "en": "Install one command, block weaponized deps"
    },
    "sub": {
      "zh": "开源（Apache-2.0）。本地跑，无账号、无模型、无推理成本。hosted-CI 版本做 PR 拦截与每日 corpus 更新。",
      "en": "Open source (Apache-2.0). Runs locally — no account, no model, no inference cost. The hosted-CI tier adds PR-blocking and daily corpus updates."
    },
    "button": {
      "zh": "pipx install promptaudit",
      "en": "pipx install promptaudit"
    }
  },
  "footer": {
    "tag": {
      "zh": "为 Coding Agent 时代做的依赖扫描",
      "en": "Dependency scanning for the Coding Agent era"
    }
  },
  "content_version": "v0.8.0"
}
